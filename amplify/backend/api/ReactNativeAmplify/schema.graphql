type Stake @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String!
}

type Condition @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String!
}
 